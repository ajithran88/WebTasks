I ended up using TheMealDB instead of Fruityvice because Fruityvice doesn't send CORS headers, so the browser blocks the request and you can't fetch it from the front-end page. 
TheMealDB works the same way so the app still meets all the requirements, just with food categories instead of fruit. 
Happy to switch it back if you'd rather I set up a small server to get around the CORS issue.
Also put all the code into one file again for simplicity and size of the project
